services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: runnerapp
      POSTGRES_USER: runner
      POSTGRES_PASSWORD: runner
    ports:
      - '5433:5432'
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
